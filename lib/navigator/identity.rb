# frozen_string_literal: true

module Navigator
  # Gem identity information.
  module Identity
    NAME = "navigator"
    LABEL = "Navigator"
    VERSION = "6.0.2"
    VERSION_LABEL = "#{LABEL} #{VERSION}"
  end
end
