# frozen_string_literal: true

module Navigator
  # Gem identity information.
  module Identity
    NAME = "navigator"
    LABEL = "Navigator"
    VERSION = "5.3.0"
    VERSION_LABEL = "#{LABEL} #{VERSION}"
  end
end
